import React, { Component } from 'react';

class Notes extends Component {
    render() {
        return(
            <header>
                Notes placeholder.
            </header>
        );
    }
}

export default Notes;