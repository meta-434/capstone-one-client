import React, { Component } from 'react';

class Timer extends Component {
    render() {
        return(
            <div>
                Timer placeholder.
            </div>
        );
    }
}

export default Timer;